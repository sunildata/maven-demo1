pipeline{
    agent any
    stages{
        stage("git cloning"){
            steps{
                git branch: 'main', url: 'https://github.com/sunildata/maven-demo1.git'
            }     
       }
    }
}
