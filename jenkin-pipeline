pipeline{
    agent any
    stages{
        stage("git cloning"){
            steps{
                git branch: 'main', credentialsId: 'git-cread', url: 'https://github.com/sunildata/maven-demo1.git'
            }     
       }
    }
}
